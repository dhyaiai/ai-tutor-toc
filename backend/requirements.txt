# Web Framework
fastapi==0.115.6
uvicorn[standard]==0.34.0
python-multipart==0.0.19

# Database
sqlalchemy[asyncio]==2.0.36
aiomysql==0.3.2
alembic==1.14.0

# Auth
python-jose[cryptography]==3.3.0
passlib[bcrypt]==1.7.4
bcrypt>=4.0,<5.0  # bcrypt 5.x 与 passlib 1.7.x 不兼容

# Redis & Cache
redis==5.2.1

# Task Queue
celery[redis]==5.4.0

# MinIO / S3
minio==7.2.10

# AI / LLM（对齐实际运行环境；视觉模型客户端依赖 2.x API）
openai==2.53.0

# LangChain 生态（仅智能出题工作流 question_pipeline 使用，与手写 ReAct Agent 并存）
langchain-core==1.4.9
langchain-openai==1.3.4
langgraph==1.2.7

# Image processing（image_preprocess.py 模块级导入 cv2/numpy，切图/分析流程必需）
opencv-python-headless==4.11.0.86  # 对齐实际运行环境；注意 4.10.x 无 Python 3.14 wheel
Pillow==11.0.0
numpy==1.26.4

# PDF 渲染（报告/订正本 HTML → PDF，首次安装后需执行: playwright install chromium）
playwright>=1.49.0

# PDF 解析 / Word 导出（fitz 解析上传的 PDF 试卷；python-docx 作文批改导出）
PyMuPDF==1.27.2.3
python-docx==1.2.0

# 语音合成（口语测评评语 TTS，edge-tts 在线合成）
edge-tts==7.2.8

# HTTP
httpx==0.28.1

# WebSocket 客户端（讯飞语音评测流式接口；uvicorn[standard] 已附带，此处显式声明）
websockets>=12.0

# Utils
cachetools==7.1.7  # 同类题生成结果 TTL 缓存（questions.py 模块级导入）
pylatexenc==2.11  # LaTeX→Unicode 文本转换（PDF/导出等非交互出口，latex_convert.py）
pydantic[email]==2.10.3
pydantic-settings==2.7.0
python-dotenv==1.0.1
