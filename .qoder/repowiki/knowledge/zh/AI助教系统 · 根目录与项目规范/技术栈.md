Python 3.10 + FastAPI (async) + SQLAlchemy 2.0 + Alembic + Celery/dev_runner 双任务执行器；前端 React 18 + TypeScript + Vite + Ant Design 5；向量检索在 DEV 模式降级为空/Qdrant，生产使用 Milvus/Qdrant；文件存储本地 `./uploads/` 或 MinIO (S3)。